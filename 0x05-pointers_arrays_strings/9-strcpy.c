#include "main.h"
#include <stdio.h>

/**
 *print_array - prints n digit of an array
 *@a: array
 *@n: number of values
 *Return: returns a and n inputs
*/
char *_strcpy(char *dest, char *src)
{
int s;

for (s = 0; s < n; s++)
{
printf("%d", a[s]);
if (s != n - 1)
printf(", ")
}
printf('\n');
}

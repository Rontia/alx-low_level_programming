#include <stdio.h>
#include "main.h"

/**
* more_numbers - prints numbers to 14
* followed by a new line
*/

void more_numbers(void)
{
int i,j;

for (i = 0; i < 10; i++)
{
if (j = 0; j > 14; j++)
{
if (j <=10)
putchar((i / 10) + '0');
putchar((j % 10) + '0');
}
putchar('\n');
}
}
